# Comprehensive Research Results & Benchmark Report (v3)

This document reports the quantitative empirical findings of the **PINN Mechanistic Interpretability** research campaign: the v2.1 hardened negative SAE result (stages 1–7) plus the **v3 regime-boundary campaign** (stages 8–13): PCA causal battery, causal-abstraction interchange battery, dimensional-boundary expansion (2D + time-dependent PDEs), the FNO operator positive control, SOTA optimization baselines, and statistical hardening. Hypotheses and decision rules for all v3 stages were preregistered in `docs/preregistration.md` before running.

The headline is unchanged — a *hardened negative mechanistic result* with a mechanistic explanation and a validated pipeline — now **generalized** (the null is basis-independent) and **bounded** (a high-rank regime where SAEs work is measured on the other side). Every number is generated by `experiments/run_pipeline.py` stages 1–13 and reproducible from `runs/`.

---

## 1. Experimental Matrix & Qualification Gate (10 seeds per regime)

Seed block `[7, 42, 123, 555, 777, 888, 999, 2024, 2025, 2026]` per regime; per-seed statistics and 95% bootstrap CIs in `runs/failure_atlas/seed_matrix_stats.json`.

| Regime | n seeds | Mean rel $L_2$ ± std | 95% CI | Label consistency |
| :--- | :--- | :--- | :--- | :--- |
| **success baseline** | 10 | 0.0358 ± 0.0515 | [0.009, 0.068] | 6 success / 3 unlabeled / 1 collocation label |
| **boundary starvation** ($\lambda_{\text{pde}}{=}100$, $\lambda_{\text{bc}}{=}0.01$) | 10 | 2.31 ± 1.59 | [1.340, 3.260] | **10/10 boundary-starvation** (reproducible failure) |
| **gradient conflict** ($\eta{=}0.1$) | 10 | 0.598 ± 0.374 | [0.352, 0.824] | 8 spectral / 1 unlabeled / 1 success — excluded from final causal battery |
| **spectral suppression** (W=16, source=50) | 10 | 0.395 ± 0.114 | [0.327, 0.469] | **10/10 spectral-suppression** (reproducible failure) |
| **collocation starvation** ($N_i{=}4$, boundary bias 0.8) | 10 | 0.0098 ± 0.0116 | [0.004, 0.019] | 7 success / 3 unlabeled — excluded from failure-class claims |

---

## 2. SAE Discovery with Mandatory Baselines

Three TopK SAEs (expansion 4×, k=8) trained on 20,000 activation vectors (5 fully-logged runs, run-level splits). Baselines evaluated alongside, per the v2 protocol.

| Model | Recon MSE | Dead features |
| :--- | :--- | :--- |
| TopK SAE replicas (×3) | 3.35–3.93 × 10⁻³ | 51–70% |
| **PCA, k-matched (8 components)** | **2.66 × 10⁻⁴** | — |
| PCA, full (64 components) | 4.9 × 10⁻⁷ | — |
| Random-encoder SAE (frozen) | 4.52 × 10⁻¹ | — |

**SAE beats random (100×) but loses to k-matched PCA (12×).** Section 5 explains *why*.

---

## 3. Physics-Feature Dictionary (multi-view, with random-direction control)

Associations against seven metric views simultaneously, with a random-direction control through the identical pipeline (`runs/feature_dictionary/random_baseline_report.json`):

| Metric view | Max real $|r|$ | Max random $|r|$ | Real beats random? |
| :--- | :--- | :--- | :--- |
| `grad_cosine` | 0.747 | 0.023 | **Yes** (32×) |
| `high_freq_power` | 0.631 | 0.022 | **Yes** (29×) |
| `loss_pde` | 0.432 | 0.022 | **Yes** (20×) |
| `rel_l2` | 0.399 | 0.021 | **Yes** (19×) |
| `loss_bc` | 0.307 | 0.019 | **Yes** (16×) |
| `boundary_dist` | 0.195 | 0.024 | **Yes** (8×) |
| `data_std` | 0.999 | 0.020 | Yes (50×) — **run-type confound, not physics** |

---

## 4. Causal Validation — the make-or-break result (v2.1 hardened)

88 feature evaluations (8 candidate features × 11 held-out boundary-starvation checkpoints), each against three controls: unrelated-feature ablation, norm-matched random-direction perturbation (per-row magnitude matched, 5-seed median in the positive control), and a **supervised linear-probe direction** (AXBench-style). Full probe coverage verified (the v2.0 run had a coverage bug where some features fell back to a random control; fixed and re-run).

### 4.1 Primary endpoint

| Quantity | Estimate | 95% bootstrap CI |
| :--- | :--- | :--- |
| **Causal strength $E_T$** | **−0.0002** | **[−0.0051, +0.0060]** |
| Specificity $S_{\text{spec}}$ | 1.23 | [0.97, 1.53] |
| Raw "beats all 3 controls" evaluations | 4/88 (4.5%) | — |
| **Bonferroni survivors** ($\alpha/m$, $m{=}8$) | **0/8** | threshold $p \le 0.00625$ |
| **BH-FDR survivors** ($q{=}0.05$) | **0/8** | chance expectation: 0.4 hits |

**Zero features survived multiple-comparison-corrected causal testing.** The $E_T$ CI includes zero; specificity straddles 1.0. The 4 raw wins at 4.5% is consistent with chance (expected ≈ 0.4–4 raw hits depending on the per-test rule); after Bonferroni or BH-FDR correction across the 8 features tested, nothing survives.

### 4.2 Sign diagnostic

All 88 target deltas are positive (ablation always *increases* PDE loss): the features encode activation energy required by the reconstruction, and *any* perturbation that degrades reconstruction shifts the loss comparably. This is the signature of features that are *representational*, not *causal* — ablating any active latent damages the decode; nothing direction-specific is present.

### 4.3 Probe-control comparison

The supervised linear-probe direction beats the SAE feature's causal effect outright in **9/88 (10.2%)** of evaluations and matches it elsewhere — AXBench-consistent (simple linear baselines ≈ SAEs), now as a measured comparison rather than an inference.

### 4.4 Positive control — the pipeline works (planted-feature experiment)

Before trusting a null, we verified the machinery can detect a *known* causal feature. Synthetic activations with planted sparse ground-truth (128 features in 64 dims, superposition regime) were run through the **exact** SAE + hook + intervention + scoring battery (`experiments/positive_control.py`, artifact `runs/positive_control.json`):

| Measurement | Value |
| :--- | :--- |
| SAE recovery of planted feature (decoder cosine) | **0.99** |
| Targeted ablation Δ target loss | **+0.024** |
| Random-direction control Δ (median of 5 trials) | 0.0003 (spread ≤ 0.0016) |
| Unrelated-feature control Δ | ~0 |
| Probe-direction control Δ | ~0 |
| **Pipeline verdict** | **PASS** — targeted effect 15× all controls |

The pipeline recovers and causally validates a planted feature when one exists. The PINN null is therefore attributable to **the PINN activations**, not to a machinery bug.

---

## 5. Why PCA wins: the activations are not in superposition (effective-rank analysis)

The most informative number in this study (`analysis/effective_rank.py`, artifact `runs/effective_rank_analysis/effective_rank_report.json`):

| Statistic (64-unit hidden layer, all runs) | Value |
| :--- | :--- |
| **Participation ratio of activation covariance** | **1.34 mean (range 1.03–1.53)** |
| PCA components for 95% energy | 1.83 mean |
| PCA components for 99% energy | ≤ 5 |
| Superposition ratio (PR / 64 dims) | **0.021** |

**The 64-dimensional hidden activations live on an effectively 1.5-dimensional manifold.** A 1D-input PINN evaluated on a 1D probe grid produces a 1-parameter family of activation vectors — a smooth curve through activation space. There is no superposition because there is nothing to superpose: the network encodes far fewer effective features than it has neurons.

This single measurement explains every negative SAE result simultaneously:
- **Why k-matched PCA beats the SAE 12×** — the data is (sub)linear and low-rank; PCA is the *correct* tool.
- **Why 51–70% of SAE latents are dead and only 1/256 features are seed-stable** — an overcomplete sparse dictionary has no genuine features to find on a rank-1.5 manifold; what it recovers is arbitrary partitions of a curve, which differ run to run (consistent with Leask et al. 2025).
- **Why causal interventions show no specificity** — latents carve up a curve; ablating any segment damages the decode without direction-specific loss effects.
- **Why `data_std` (run identity) is the strongest correlate** — the activation curve's *thickness* separates regimes, and everything else is geometry along one dimension.

**Implication for the field:** interpretability tools validated on LLM residual streams (where superposition is abundant) may not transfer to small scientific networks trained on narrow, low-dimensional objectives. We state this as a **scoped negative result**: nothing here rules out SAEs on wide PINN ensembles, higher-dimensional PDE parameterizations, or operator-learning models (FNOs, DeepONets) where the effective rank of representations is plausibly much higher. Testing that boundary is the natural follow-up.

### 5.1 Width-scaling follow-up

To test whether raw width alone creates superposition, the fixed 1D Poisson task was trained for 1,000 steps at widths 16, 32, 64, 128, 256, and 512. The local tangent rank was 1 at every width, while covariance PR stayed between 1.74 and 2.00:

| Width | PR | Stable rank | Local tangent rank | PCA components (95% / 99%) |
| :---: | ---: | ---: | ---: | ---: |
| 16 | 2.000 | 1.349 | 1 | 2 / 3 |
| 32 | 1.895 | 1.278 | 1 | 2 / 3 |
| 64 | 1.823 | 1.217 | 1 | 2 / 3 |
| 128 | 1.740 | 1.175 | 1 | 2 / 2 |
| 256 | 1.965 | 1.432 | 1 | 2 / 3 |
| 512 | 1.795 | 1.119 | 1 | 3 / 4 |

This supports the narrower claim that increasing width without increasing task or input complexity did not produce observed superposition in this benchmark. It is a single-seed follow-up and does not establish a universal width law. Artifact: `runs/width_scaling/width_scaling_report.json`.

### 5.2 Two-dimensional boundary pilot

The manufactured 2D Poisson prototype was run for five seeds with the same 32-wide, three-hidden-layer architecture and 1,000 training steps. The corrected forcing term was required to prevent the trivial zero solution. Across seeds, local tangent rank was 2 and participation ratio was 2.89–3.17 (mean 3.03), compared with 1.74–2.00 in the 1D width study:

| Dimension | Seeds | Mean PR | PR range | Tangent ranks | Mean final rel $L_2$ |
| :---: | ---: | ---: | ---: | :---: | ---: |
| 1D width study | 6 | — | 1.74–2.00 | 1 | — |
| 2D Poisson | 5 | 3.03 | 2.89–3.17 | 2 | 0.211 |

This is evidence that increasing domain dimension raises the observed covariance effective rank while preserving the expected local tangent-dimension distinction. It is a boundary-mapping pilot, not evidence that SAEs are causally useful in 2D; no 2D SAE or intervention battery has been run. Artifact: `runs/dimensional_boundary_2d/dimensional_boundary_report.json`.

---

## 5A. v3 Campaign Results (Stages 8–13)

### 5A.1 PCA causal battery — the negative result is basis-independent (Stage 8)

The identical 3-control causal protocol (unrelated-component, random-coefficient-direction, supervised-probe controls; per-feature exact sign tests; Bonferroni + BH-FDR corrections; same 11 boundary-starvation checkpoints; same scoring code) was run on the **leading 8 PCA components** of the activation manifold — the "linear methods are the right tool" candidate (`experiments/pca_causal.py`, artifact `runs/pca_causal_results.json`):

| Quantity | SAE features (v2.1) | PCA components (v3) |
| :--- | :--- | :--- |
| Causal strength $E_T$ (mean) | −0.0002 [−0.0051, +0.0060] | **−0.0186 [−0.0301, −0.0092]** |
| Specificity $S_{\text{spec}}$ | 1.23 [0.97, 1.53] | straddles 1 (chance) |
| Sign diagnostic | 88/88 positive | **88/88 positive** |
| Bonferroni / BH-FDR survivors | 0/8 | **0/8** |
| Probe control beats target | 10.2% | 34.1% |
| Paired PCA−SAE $E_T$ diff | — | −0.0185 [−0.0313, −0.0062] |

**Interpretation.** The guide's Phase-10 hypothesis ("PCA components are the causal features; SAEs merely fragmented them") is **falsified**. PCA components carry *larger* reconstruction-relevant footprints (bigger target deltas) but the same representational (all-positive) signature and no direction-specific causal information: ablating any leading component damages the decode exactly as ablating any active SAE latent does. **The correct conclusion is stronger than the v2.1 claim: no feature basis — linear or sparse — provides direction-specific causal interpretability for these PINN activations.** The positive control (planted feature) still passes, so this is attributable to the activations, not the machinery. Preregistered as H8a vs H8b in `docs/preregistration.md`; H8a confirmed.

### 5A.2 Causal abstraction — no candidate alignment satisfies the interchange criterion (Stage 9)

Region-level **partial interchange interventions** (Geiger et al. 2021/2022 formalism) test whether an aligned subspace causally abstracts the high-level model $H_{\text{boundary}}$: *{region ∈ interior, boundary-proximal} → PDE loss*. Swapping the aligned-subspace coefficients of an interior batch with a boundary-proximal batch's region-mean should move the source loss toward the donor loss if the alignment carries region identity. Movement fraction = $(\log L_{\text{swap}} - \log L_{\text{src}})/(\log L_{\text{don}} - \log L_{\text{src}})$; the gate is *beating the random orthonormal basis on every run* (`experiments/causal_abstraction.py`, artifact `runs/causal_abstraction_results.json`):

| Alignment basis | Mean movement | Diff vs random | Runs positive | Passes? |
| :--- | ---: | ---: | :---: | :---: |
| PCA (top-8) | +7.47 | −0.08 | 1–2/11 | **No** |
| SAE decoder (top-8 by activity) | +7.45 | −0.10 | 1/11 | **No** |
| Random orthonormal | +7.55 | — | — | (control) |

The movement is large and positive for **every** basis — because swapping 8 of 64 coefficients injects donor-region energy through the shared low-rank geometry — but no candidate basis exceeds the random control with run-level consistency. **Neither linear nor sparse alignments are causal abstractions of the region-identity variable.** The interchange criterion converts "features are representational" into a formal statement: the activation manifold's coordinates do not align with the high-level causal variable in any basis tested.

### 5A.3 Dimensional boundary — expanded PDE suite (Stage 10)

New PDE families (manufactured-solution 2D suite + time-dependent Burgers/Allen-Cahn with enforced initial conditions and stable integrating-factor reference solvers) measure the same geometry statistics (3 seeds each; `experiments/dimensional_boundary_expanded.py`, artifact `runs/dimensional_boundary_expanded/`):

| PDE family | Input dim | Mean PR | PR range | Tangent rank | Final rel $L_2$ |
| :--- | :---: | ---: | ---: | :---: | ---: |
| Poisson 1D (width study, ref) | 1 | 1.87 | 1.74–2.00 | 1 | — |
| Poisson 2D (pilot, ref) | 2 | 3.03 | 2.89–3.17 | 2 | 0.211 |
| **Advection-Diffusion 2D** | 2 | **2.47** | 2.39–2.53 | **2** | 0.025 |
| **Reaction-Diffusion 2D** | 2 | **2.29** | 2.07–2.67 | **2** | 1.92 |
| **Burgers 1D (t,x)** | 2 | **1.63** | 1.32–1.90 | **2** | 6.1 |
| **Allen-Cahn 1D (t,x)** | 2 | **1.51** | 1.34–1.58 | **2** | 6.0 |

Two findings: (i) **local tangent rank exactly saturates the input-dimension bound in every family** — the provable statement in `docs/theory_activation_rank.md` — while covariance PR stays in the 1.3–3.2 band (no superposition onset from dimension alone at these widths); (ii) time-dependent inputs (t,x) show PR *below* steady 2D: training explores an effectively one-parameter family of solution states on the (t,x) domain at these schedules. The theory document also records an explicit correction: covariance rank is **not** bounded by input dimension (the v2 planning guide's "Whitney" sketch is false; moment-curve counterexample), so PR remains an empirical diagnostic and the tangent rank is the only rigorous bound.

### 5A.4 The operator regime boundary — SAEs work where representations are high-rank (Stage 11)

A dependency-free **FNO1d** (width 64, 4 spectral layers, 12 modes) trained on parametric **Green's-function regression** (Gaussian-mixture forcing functions → nonlinear spectral response; 2,048 training functions; the linear-only variant is solved exactly by one spectral layer, so a mode-mixing nonlinearity is required to make the task non-trivial). Representations = per-(sample, grid-position) block states — the analogue of token-level residual activations (`experiments/operator_boundary.py`, artifact `runs/operator_boundary/operator_boundary_report.json`):

| Representation | PR | W | PR/W | PCA comps (95%/99% energy) | Recon: TopK SAE vs PCA(k=8) |
| :--- | ---: | ---: | ---: | :---: | :--- |
| PINN hidden acts (1D suite) | 1.34–2.0 | 64 | **0.02–0.03** | 2 / 3 | **PCA wins 12×** |
| 2D + time-dependent PINNs | 1.5–3.2 | 32–64 | 0.02–0.10 | 2–3 / 3–5 | PCA wins (2D pilot) |
| **FNO block states (Green's)** | **6.80** | 64 | **0.106** | **19 / 38** | **SAE wins 4.7×** (7.0e-4 vs 3.3e-3) |

This is the **regime boundary, measured on both sides with the same SAE and the same protocol**: function-space representations are genuinely higher-rank (PR 5× the PINNs, 19 PCA components for 95% energy), and there the TopK SAE *beats* k-matched PCA on reconstruction — the superposition-regime signature (Elhage et al. 2022) that is absent from every PINN layer measured. The claim is scoped to reconstruction quality; a causal battery on operator features is future work. Preregistered as H4a vs H4b; H4a confirmed.

### 5A.5 SOTA optimization baselines vs the controller (Stage 12)

Three literature-standard adaptive-training baselines on the same boundary-starvation configuration as the stage-7 controller demo (`experiments/sota_baselines.py`, artifact `runs/sota_baselines/sota_baseline_report.json`):

| Method | Final rel $L_2$ | vs controller (0.0166) |
| :--- | ---: | :--- |
| GradNorm (gradient equalization) | 2.0134 | loses (worse than no-action 0.295 — actively harmful here) |
| **NTK-adaptive weighting (Wang 2021)** | **0.0064** | **beats controller and oracle (0.0119)** |
| RBA (residual-based attention) | 2.1918 | loses (actively harmful) |
| Closed-loop controller (ours) | 0.0166 | — |
| No-action | 0.2953 | reference |
| Oracle static reweight | 0.0119 | reference |

Honest positioning: instantaneous NTK-ratio reweighting is better suited to this static misweighting failure than the controller's conservative bounded actions, and the controller is not claimed as optimization SOTA. The controller's distinct claims remain: failure-agnostic machinery with rollback, measured monitor-source ablation, and no requirement to know the correct reweighting in advance. GradNorm and RBA actively harm in this regime — equalization and residual-attention do not address starvation of a boundary term.

### 5A.6 Statistical hardening (Stage 13)

From `analysis/statistical_hardening.py` (artifact `runs/statistical_hardening/analysis_report.json`):

- **Power analysis (both causal batteries):** with 11 runs per feature, the minimum detectable sign-agreement for 80% power at α=0.05 is **85.7%**. Observed per-feature positive-difference rates are 27–55% (SAE) and 9–55% (PCA) — nowhere near detectability. The null is not an artifact of an underpowered design *for the effects that could exist at this sample size*; detecting subtler causal features would require ~4× the checkpoints (stated as the honest limitation, not hidden).
- **Bayesian posterior (monitor comparison):** P(SAE-augmented monitor better than conventional) = **0.54** — no evidence of an SAE advantage; consistent with the overlapping-CIs reading.
- **Failure-label threshold sensitivity:** boundary-starvation and spectral-suppression label fractions are invariant across thresholds 0.02–0.20 (1.00 at every threshold); the excluded `success` regime is threshold-sensitive (0.3→0.0), confirming its exclusion from class-level claims; gradient-conflict is mildly sensitive (0.9→0.7).

---

## 6. Early-Warning Monitors (run-level splits, leakage-audited, with CIs)

Trained on 40 runs, evaluated on **18 held-out runs**, failure steps derived from each run's own trajectory. Leakage audit: 0 violations. **Run-level bootstrap CIs** on AUROC (2,000 resamples of held-out runs — the independent unit):

| Monitor | AUROC | AUROC 95% CI | AUPRC |
| :--- | :--- | :--- | :--- |
| Loss-only threshold | 0.468 | [0.406, 0.511] | 0.233 |
| Conventional logistic | 0.872 | [0.795, 0.953] | 0.748 |
| SAE + conventional | **0.878** | **[0.806, 0.958]** | 0.755 |

The conventional and SAE-augmented monitors are clearly above the loss-only threshold on this held-out split; their CIs overlap, so the small SAE increment is not established. The loss-only monitor remains compatible with chance. These results support predictive monitoring of the configuration-determined failure mixture, but do not establish that SAE features add predictive value beyond conventional signals.

---

## 7. Closed-Loop Controller — rescue, baselines, and the monitor-source ablation

The v1 controller demo reported a "recovery" to $L_2 = 0.579$ (worse than its 0.279 start). Root causes (no-op rollback, untracked rel_l2, underpowered rebalancing) were fixed in v2; v2.1 adds the **monitor-source ablation** that measures — rather than asserts — what drives the rescue.

### 7.1 Three-arm rescue comparison

| Arm | Final rel $L_2$ | Best | Interventions | Rollbacks |
| :--- | :--- | :--- | :--- | :--- |
| **Closed-loop controller** (conventional signals) | **0.0166** | 0.0116 | 8 | 1 |
| No-action | 0.2953 | 0.2789 | 0 | 0 |
| Oracle reweight ($\lambda{=}$1,1 from step 0) | 0.0119 | 0.0119 | 0 | 0 |

### 7.2 Monitor-source ablation (v2.1)

Identical controller and actions; only the alarm source differs — SAE feature activity vs a **matched-dimensionality random-feature monitor** (fixed random projection, same top-8 sparsity budget):

| Arm | Final rel $L_2$ | Interventions |
| :--- | :--- | :--- |
| SAE-feature monitor | 0.0236 | 10 |
| Random-feature monitor | 0.0236 | 10 |
| Conventional (loss/$L_2$) monitor | **0.0166** | 8 |

The SAE- and random-feature arms produce **bit-identical rescue trajectories** (verified equal at every step, `controller_comparison.json: trajectories_bit_identical=true`). Both activity signals rise with training instability, and the bounded rebalancing action is rate-limited by cooldown and confirmation — not by the monitor's fine structure. The conventional loss/$L_2$ monitor is *slightly better* (fewer interventions, lower final error).

**Measured conclusion:** the SAE features are inert cargo in the closed loop. The rescue is attributable to the controller machinery driven by any instability-correlated signal — with the conventional signal best. This ablation converts "driven by conventional signals" from an inference into a citable measurement.

### 7.3 Honest scope

This validates the *controller engineering* (bounded actions, genuine rollback, verifiable per-step logging, rescue vs no-action/oracle baselines). It is **not** evidence for SAE features, and the two claims are kept structurally separate throughout.

---

## 8. Claim ladder — where the evidence lands (v3)

1. **Representational claim — WEAKENED TO EXPLAINED-NULL.** Strong directional associations with physics quantities exist (r = 0.20–0.75, beating random controls 8–32×), but the effective-rank analysis shows the activation manifold is a ~1.5-dimensional curve: the "features" are coordinates along geometry, not superposed concepts. The strongest association (`data_std`, r = 0.999) is a labeled run-type confound.
2. **Causal claim — REJECTED (hardened negative).** $E_T$ CI includes zero; 0/8 features survive Bonferroni *or* BH-FDR; all target deltas sign-consistent with representational (not causal) encoding; probe directions match or beat SAE features. The planted-feature positive control PASSES, so the null is attributable to the PINN activations, not the pipeline.
3. **Causal claim v3 — GENERALIZED BEYOND SAEs.** The same battery on PCA components: 0/8 survive, same all-positive representational signature, paired PCA−SAE difference negative. **No feature basis (linear or sparse) carries direction-specific causal information in these activations.** The causal-abstraction interchange test independently confirms: neither PCA nor SAE alignments beat a random basis for region identity.
4. **Geometry claim — MEASURED ACROSS THE DIMENSIONAL AND ARCHITECTURE BOUNDARY.** Local tangent rank exactly equals input dimension in all six PDE families and all widths (the provable bound, saturated); covariance PR stays low (1.3–3.2) for every PINN including time-dependent (t,x) inputs; the FNO's function-space representations have PR 6.8 (PR/W 0.106) and **there the SAE beats k-matched PCA 4.7×** — the superposition regime exists and SAE methodology is appropriate in it. The boundary is measured, not asserted.
5. **Predictive claim — SUPPORTED WITH SCOPE.** Conventional and SAE-augmented monitors discriminate the held-out failure mixture (AUROC 0.872 and 0.878), while the loss-only threshold is near chance; overlapping CIs and P(SAE better)=0.54 do not show an SAE advantage.
6. **Prevention claim — SUPPORTED FOR CONTROLLER MACHINERY ONLY, AND NOW MEASURED.** The closed-loop controller rescues boundary starvation (0.295 → 0.017; beats no-action 18×; within 1.4× of oracle), and the monitor-source ablation proves the SAE features contribute nothing — the rescue runs on conventional signals. **The controller is not optimization SOTA:** NTK-adaptive weighting reaches 0.0064 on this failure (better than oracle); GradNorm and RBA actively harm. The controller's value is failure-agnosticism + rollback + no foreknowledge of the correct reweighting.

**The paper this evidence supports:** *"We built and preregistered a full mechanistic-interpretability pipeline for PINN failure diagnosis — failure atlas, TopK SAEs with mandatory PCA/random/probe controls, planted-feature positive control, multi-view feature dictionary, multiple-comparison-corrected causal battery, causal-abstraction interchange battery, leakage-audited monitors with run-level CIs, and a rollback-capable closed-loop controller — and found that SAE-derived features provide no causal information beyond matched controls in this benchmark. The null is basis-independent: PCA components fail the identical battery and no candidate alignment satisfies the interchange criterion for region identity. Effective-rank analysis explains why: these activations are not in superposition (participation ratio ≈ 1.3 of 64 dimensions; local tangent rank exactly equals input dimension across six PDE families), so sparse dictionary learning solves a problem that does not exist — and in a Fourier neural operator trained on function-space regression, where PR/W reaches 0.106, the same SAE beats k-matched PCA 4.7×. The regime boundary is measured on both sides. A conventional-signal-driven controller meaningfully reduces failure rate independently of interpretability, with NTK-adaptive weighting the stronger specialized alternative for known misweighting failures."*

**Limitations (scoped, explicit):** this does not rule out SAEs on wider PINNs, ensembles, or higher-dimensional parameterizations; the FNO positive control is reconstruction-level (no operator causal battery yet); the operator/2D/time-dependent geometry runs are 1–5 seed pilots; the causal batteries at 11 runs/feature can only detect ≥85.7% sign-agreement effects at 80% power; the monitor result is specific to this held-out failure mixture and window protocol; gradient-conflict and collocation-starvation were stress-tested but excluded from final failure-class claims because their operational labels did not reproduce cleanly; covariance participation ratio is an empirical diagnostic, not a theorem (the tangent-rank bound is the provable statement — see `docs/theory_activation_rank.md`).

---

## Reproduction

```bash
python -m pytest tests/                                   # 139 tests, all passing
python -m experiments.run_pipeline --stages 2            # failure atlas + 10-seed stats
python -m experiments.run_pipeline --stages 3            # TopK SAEs + PCA/random baselines
python -m experiments.run_pipeline --stages 4            # multi-view dictionary + random control
python -m experiments.run_pipeline --stages 5            # causal battery: 3 controls + MC corrections + positive control
python -m experiments.run_pipeline --stages 6            # monitor suite + run-level AUROC CIs
python -m experiments.run_pipeline --stages 7            # controller + monitor-source ablation
python -m experiments.run_pipeline --stages 8            # PCA causal battery (head-to-head vs SAE)
python -m experiments.run_pipeline --stages 9            # causal abstraction (partial interchange)
python -m experiments.run_pipeline --stages 10           # 2D suite + time-dependent geometry
python -m experiments.run_pipeline --stages 11           # FNO operator regime boundary
python -m experiments.run_pipeline --stages 12           # SOTA baselines vs controller
python -m experiments.run_pipeline --stages 13           # statistical hardening
```

Key artifacts: `runs/failure_atlas/seed_matrix_stats.json` · `runs/sae_models_v2/sae_stage3_summary.json` · `runs/feature_dictionary/` · `runs/causal_intervention_results.json` (incl. `multiple_comparisons`, `sign_diagnostic`, `probe_control`) · `runs/positive_control.json` · `runs/effective_rank_analysis/effective_rank_report.json` · `runs/monitor_report.json` · `runs/controller_demo/controller_comparison.json` · `runs/pca_causal_results.json` · `runs/causal_abstraction_results.json` · `runs/dimensional_boundary_expanded/dimensional_boundary_expanded_report.json` · `runs/operator_boundary/operator_boundary_report.json` · `runs/sota_baselines/sota_baseline_report.json` · `runs/statistical_hardening/analysis_report.json`.